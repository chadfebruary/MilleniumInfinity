package com.milleniuminfinity.app.milleniuminfinity.domain.internet;

import android.test.AndroidTestCase;

import junit.framework.Assert;

/**
 * Write a description of class ADSL here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */

public class ADSLTest extends AndroidTestCase
{
    /*public void testName() throws Exception {
        ADSL adsl = new ADSL.Builder()
                .ipAddress("ipAddress")
                .ISP("ISP")
                .planName("PlanName")
                .price("Price")
                .dataAllowance("DataAllowance")
                .build();

        Assert.assertEquals(adsl.getIPAddress(), "ipAddress");
        Assert.assertEquals(adsl.getISP(), "ISP");
        Assert.assertEquals(adsl.getPlanName(), "PlanName");
        Assert.assertEquals(adsl.getPrice(), "Price");
        Assert.assertEquals(adsl.getDataAllowance(), "DataAllowance");
    }*/

}
